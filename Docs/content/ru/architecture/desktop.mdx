---
title: HyPrism.Desktop
description: Avalonia views, view models, локализация и нативные контролы
---

# HyPrism.Desktop

`HyPrism.Desktop` является активным хостом приложения

## Состав

- `App.axaml` подключает общие токены, стили и ресурсы
- `MainWindow` предоставляет нативное управление окном и основную навигацию
- `MainWindowViewModel` координирует главную и новости через сервисы Core
- `SettingsViewModel` адаптирует `ISettingsService` для интерфейса настроек
- пользовательские контролы отвечают за rich content новостей, плавный скролл и анимированные комбобоксы
- `.resx`-файлы в `Localization` содержат строки интерфейса для поддерживаемых культур

В Desktop по умолчанию включены compiled bindings. View model предоставляет состояние и команды, а code-behind используется только для поведения, зависящего от ввода, лейаута или анимаций Avalonia

Платформенные сервисы, включая файловые диалоги, регистрируются хостом через callback Bootstrapper
